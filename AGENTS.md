# 南网以太网103协议库开发计划

> 本项目定位：**南网以太网103协议解析与通信库**
> 
> 协议本质：**IEC 104 的 APCI + IEC 103 的 ASDU**

---

## 一、协议核心差异

### 1.1 与标准IEC 103的关键差异

| 特性 | 标准 IEC 103 | 南网以太网103 |
|------|-------------|--------------|
| **传输层** | 串口 RS485 | TCP/IP（端口2404） |
| **APCI** | 无 | **4字节控制域**（参照104） |
| **帧格式** | FT1.2固定/可变帧 | 仅可变帧（68H开头） |
| **长度域** | 1字节 | **2字节**（最大2045） |
| **ASDU地址** | 1字节 | **2字节**（设备地址+CPU号） |
| **时标** | CP32Time2a(4字节) | **CP56Time2a(7字节)** |
| **遥测/遥脉传输** | ASDU9/15 | **通用服务ASDU10** |
| **配置读取** | 无标准 | **通用服务ASDU21/10** |

### 1.2 APCI控制域格式

```
I格式（信息传输）- 用于传输ASDU:
┌────────────────────────────────────────────────────┐
│ 字节1: N(S)低8位                              LSB=0 │
│ 字节2: N(S)高7位 + 保留位                           │
│ 字节3: N(R)低8位                              LSB=0 │
│ 字节4: N(R)高7位 + 保留位                           │
└────────────────────────────────────────────────────┘
发送序号N(S)和接收序号N(R)用于防止报文丢失和重复

S格式（监视确认）- 用于确认I格式:
┌────────────────────────────────────────────────────┐
│ 字节1: 00000001                                    │
│ 字节2: 00000000                                    │
│ 字节3: N(R)低8位                              LSB=0 │
│ 字节4: N(R)高7位 + 保留位                           │
└────────────────────────────────────────────────────┘

U格式（控制功能）- 启动/停止/测试:
┌────────────────────────────────────────────────────┐
│ 字节1: TESTFR | STOPDT | STARTDT | 000011          │
│        确认/生效 | 确认/生效 | 确认/生效             │
│ 字节2-4: 00000000                                  │
└────────────────────────────────────────────────────┘

STARTDT(启动): 0x07  STARTDT确认: 0x0B
STOPDT(停止):  0x13  STOPDT确认:  0x23
TESTFR(测试):  0x43  TESTFR确认: 0x83
```

### 1.3 流量控制参数 (k/w)

> **IEC 104 标准核心机制**：通过滑动窗口实现可靠传输

#### 1.3.1 参数定义

| 参数 | 含义 | 默认值 | 范围 |
|-----|------|-------|------|
| **k** | 未被确认的I格式APDU最大数目 | 12 | 1 ~ 32767 |
| **w** | 最迟确认APDU的最大数目 | 8 | 1 ~ 32767 |

**建议配置**：w ≤ 2k/3

#### 1.3.2 工作原理

```
主站 (发送方)                         子站 (接收方)
     │                                     │
     │───── I(0) ────────────────────────>│
     │───── I(1) ────────────────────────>│
     │───── I(2) ────────────────────────>│ ← 收到w个后必须发S帧确认
     │        ...                          │
     │───── I(k-1) ──────────────────────>│ ← 发送满k个后必须暂停
     │                                     │
     │<──── S(N(R)=k) ─────────────────────│ ← 子站发送确认
     │                                     │
     │───── I(k) ────────────────────────>│ ← 收到确认后继续发送
```

#### 1.3.3 发送方行为（k值控制）

```
发送I帧前:
┌─────────────────────────────────────────────────────┐
│ if (unconfirmedCount() >= k) {                      │
│     // 暂停发送，等待确认                            │
│     // 可设置超时，超时则断开连接                    │
│     return;                                         │
│ }                                                   │
│ // 正常发送                                         │
│ sendIFrame(...);                                    │
└─────────────────────────────────────────────────────┘
```

#### 1.3.4 接收方行为（w值控制）

```
收到I帧后:
┌─────────────────────────────────────────────────────┐
│ recvCount++;                                        │
│ if (recvCount >= w) {                               │
│     // 立即发送S帧确认                              │
│     sendSFrame(recvSeq);                            │
│     recvCount = 0;                                  │
│ } else {                                            │
│     // 启动T2超时，超时后发送确认                   │
│     startAckTimer();                                │
│ }                                                   │
└─────────────────────────────────────────────────────┘
```

#### 1.3.5 与T2超时的关系

| 机制 | 触发条件 | 说明 |
|-----|---------|------|
| **w计数** | 收到w个I帧 | 立即确认，响应快 |
| **T2超时** | T2时间到期 | 兜底确认，防止丢确认 |

**完整实现应同时支持两种机制**：
- 收到w个I帧后立即发S帧
- 若w计数未触发但T2超时，也发S帧

#### 1.3.6 实现状态

| 功能 | 状态 | 代码位置 |
|-----|------|---------|
| k/w 常量定义 | ✅ 已实现 | `Constants.h` |
| 未确认计数 | ✅ 已实现 | `SeqManager::unconfirmedCount()` |
| **k值发送控制** | ✅ 已实现 | `IEC103Master::generalInterrogation()` / `readGenericGroup()` |
| **w值确认触发** | ✅ 已实现 | `IEC103Master::processIFrame()` |
| T2超时确认 | ✅ 已实现 | `IEC103Master::onAckTimeout()` |

#### 1.3.7 实现代码示例

**k值发送控制** (`IEC103Master.cpp`):
```cpp
void IEC103Master::generalInterrogation(uint16_t deviceAddr) {
    if (!isConnected()) {
        Logger::warning("Cannot send GI, not connected");
        return;
    }

    // k值控制：未确认帧数超过k时暂停发送
    uint16_t unconfirmed = m_seqManager.unconfirmedCount();
    if (unconfirmed >= m_config.maxUnconfirmed) {
        Logger::warning(QString("[k-Control] Send blocked: unconfirmed=%1 >= k=%2")
                        .arg(unconfirmed).arg(m_config.maxUnconfirmed));
        return;
    }
    // ... 正常发送逻辑 ...
}
```

**w值确认触发** (`IEC103Master.cpp`):
```cpp
void IEC103Master::processIFrame(const Frame& frame) {
    // ... 序号验证 ...
    
    m_seqManager.nextRecvSeq();

    // w值控制：收到w个I帧后立即确认
    m_seqManager.incrementRecvCount();
    uint8_t recvCount = m_seqManager.recvCount();
    
    if (recvCount >= m_config.maxAckDelay) {
        sendAck();
        m_seqManager.resetRecvCount();
        stopAckTimer();
    } else {
        startAckTimer();  // T2兜底
    }
    // ... ASDU处理 ...
}
```

### 1.4 协议合规性规则

> **IEC 104 标准关键错误处理规则**

#### 1.4.1 序号验证规则

| 场景 | 验证内容 | 错误处理 |
|-----|---------|---------|
| **接收I帧** | N(S) 必须等于期望的接收序号 | 断开连接 |
| **接收S帧** | N(R) 不能大于已发送序号 | 断开连接 |
| **I帧确认** | N(R) 在有效范围内确认 | 断开连接 |

```
正确行为:
┌─────────────────────────────────────────────────────┐
│ 收到 I 帧: N(S) 必须等于 recvSeq                    │
│   - 正确: 递增 recvSeq, 处理 ASDU                   │
│   - 错误: 断开连接, 重新同步                        │
│                                                     │
│ 收到 S 帧: N(R) 必须 ≤ sendSeq                      │
│   - 正确: 更新 lastAckSeq, 减少未确认计数           │
│   - 错误: 对方确认了未发送的帧, 断开连接            │
└─────────────────────────────────────────────────────┘
```

#### 1.4.2 超时处理规则

| 超时 | 参数 | 触发条件 | 处理方式 |
|-----|-----|---------|---------|
| **T1** | 15秒 | 发送APDU后未收到响应 | 断开连接 |
| **T2** | 10秒 | 收到I帧后未发确认 | 发送S帧确认 |
| **T3** | 20秒 | 空闲时发送TESTFR | 等待确认 |
| **TESTFR超时** | T1 | 发送TESTFR后未收到确认 | 断开连接 |

#### 1.4.3 连接状态机

```
                    TCP连接成功
                         │
                         ▼
┌─────────────┐    STARTDT_ACT    ┌─────────────┐
│  Connected  │ ────────────────> │   Started   │
│  (未启动)   │                   │   (已启动)  │
└─────────────┘                   └─────────────┘
       │                                 │
       │ 断开                            │ 断开
       │                                 │
       ▼                                 ▼
┌─────────────┐                   ┌─────────────┐
│Disconnected │ <─────────────────│Disconnected │
└─────────────┘   任何错误/超时   └─────────────┘
```

#### 1.4.4 错误断开条件

| 错误类型 | 说明 | 处理 |
|---------|------|-----|
| 序号错误 | N(S) ≠ 期望值，N(R) > 发送值 | 立即断开 |
| TESTFR超时 | 发送测试帧后T1内无响应 | 立即断开 |
| T1超时 | 发送命令后T1内无响应 | 立即断开 |
| k值超时 | 未确认帧达到k值且超时未收到确认 | 立即断开 |
| 帧解析失败 | 无法解析的帧格式 | 立即断开 |

### 1.5 ASDU地址结构

```
┌─────────────────────────────────────────────────┐
│ 高字节 (设备地址)    │ 低字节                    │
│                      │ D8-D5: 定值区号           │
│                      │ D2-D0: CPU号              │
├─────────────────────────────────────────────────┤
│ 0x0000 = 子站本身                               │
│ 0x0001-0x00FE = 装置编号                        │
│ 0x00FF = 广播地址                               │
│ 0xFF00-0xFFFE = 定值区号+装置                   │
└─────────────────────────────────────────────────┘
```

---

## 二、库设计目标

### 2.1 核心定位
- 纯协议库，不包含业务逻辑
- 支持南网以太网103规范（上海许继）
- 仅实现三遥数据采集
- 不实现控制功能和特殊数据召唤

### 2.2 设计原则
- **分层架构**：传输层 → APCI层 → ASDU层 → API层
- **序号管理**：实现I格式序号机制
- **通用服务**：重点实现ASDU10/21编解码
- **易集成**：提供简洁的回调接口

---

## 三、模块架构

### 3.1 库架构 (libiec103)

```
┌─────────────────────────────────────────────────────────┐
│                     API Layer                            │
│                  IEC103Master                            │
│  ┌─────────────┬──────────────┬──────────────┐          │
│  │  connect()  │   poll()     │  GI()        │          │
│  └─────────────┴──────────────┴──────────────┘          │
│  ┌─────────────┬──────────────┬──────────────┐          │
│  │ setDataHandler() │ readGenericGroup() │   │          │
│  └─────────────┴──────────────┴──────────────┘          │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────┐
│                    ASDU Layer                            │
│  ┌──────────────┬──────────────┬──────────────┐         │
│  │ ASDUParser   │ GenericSvc   │ DataParser   │         │
│  │ (ASDU解析)    │ (通用服务)    │ (数据解析)    │         │
│  └──────────────┴──────────────┴──────────────┘         │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────┐
│                    APCI Layer                            │
│  ┌──────────────┬──────────────┬──────────────┐         │
│  │ FrameCodec   │ SeqManager   │ UFormatCtrl  │         │
│  │ (帧编解码)    │ (序号管理)    │ (U格式控制)   │         │
│  └──────────────┴──────────────┴──────────────┘         │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────┐
│                   Transport Layer                        │
│  ┌──────────────┬──────────────┬──────────────┐         │
│  │ TcpTransport │  Reconnector │  ByteBuffer  │         │
│  │ (TCP传输)     │  (重连机制)   │  (缓冲区)    │         │
│  └──────────────┴──────────────┴──────────────┘         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 应用架构

```
┌─────────────────────────────────────────────────────────┐
│                   Console Application                    │
│                                                         │
│  ┌─────────────────────────────────────────────────┐    │
│  │                    main.cpp                      │    │
│  │  - 解析命令行参数                                 │    │
│  │  - 加载配置文件                                   │    │
│  │  - 创建 IEC103Master 实例                        │    │
│  │  - 注册 DataPrinter 回调                         │    │
│  │  - 运行事件循环                                   │    │
│  └─────────────────────────────────────────────────┘    │
│                          │                              │
│  ┌─────────────────────────────────────────────────┐    │
│  │                  DataPrinter                     │    │
│  │  - 实现 IDataHandler 接口                        │    │
│  │  - 打印遥信/遥测/遥脉数据                         │    │
│  │  - 输出链路状态变化                               │    │
│  └─────────────────────────────────────────────────┘    │
│                          │                              │
│  ┌─────────────────────────────────────────────────┐    │
│  │                    Config                        │    │
│  │  - 解析 JSON/INI 配置文件                        │    │
│  │  - 主站地址、端口、轮询间隔等                     │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                          │ 调用
                          ▼
              ┌─────────────────────┐
              │    libiec103.a      │
              │   (核心协议库)       │
              └─────────────────────┘
```

### 3.3 Python测试子站架构

```
┌─────────────────────────────────────────────────────────┐
│                   Python Slave Simulator                 │
│                                                         │
│  ┌─────────────────────────────────────────────────┐    │
│  │                   slave.py                       │    │
│  │  - TCP Server (监听端口2404)                     │    │
│  │  - 处理主站连接                                   │    │
│  │  - 响应 STARTDT/STOPDT/TESTFR                    │    │
│  │  - 分发ASDU请求到处理器                           │    │
│  └─────────────────────────────────────────────────┘    │
│                          │                              │
│  ┌─────────────────────────────────────────────────┐    │
│  │                 protocol/                        │    │
│  │  - apci.py:  APCI编解码 (I/S/U格式)             │    │
│  │  - asdu.py:  ASDU编解码 (1/2/7/8/10/21/42)      │    │
│  │  - frame.py: 帧封装/解析                        │    │
│  └─────────────────────────────────────────────────┘    │
│                          │                              │
│  ┌─────────────────────────────────────────────────┐    │
│  │                   data/                          │    │
│  │  - digital.py: 模拟遥信数据生成                  │    │
│  │  - generic.py: 模拟通用服务数据(遥测/遥脉)       │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

---

## 四、项目结构

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Console App                          │
│                    (控制台应用程序)                       │
│                    main.cpp                             │
└─────────────────────────────────────────────────────────┘
                          │ 依赖
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    libiec103                             │
│                 (核心协议库 - 静态库)                     │
│  ┌─────────────────────────────────────────────────┐    │
│  │              IEC103Master (API)                  │    │
│  └─────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────┐    │
│  │              ASDU Layer                          │    │
│  └─────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────┐    │
│  │              APCI Layer                          │    │
│  └─────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────┐    │
│  │              Transport Layer                     │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                          ▲ 测试
                          │
┌─────────────────────────────────────────────────────────┐
│                    Python Slave                          │
│                 (测试用子站模拟器)                        │
└─────────────────────────────────────────────────────────┘
```

### 4.2 目录结构

```
xuji103/
├── lib/                              # 核心协议库 (libiec103)
│   ├── include/
│   │   └── iec103/
│   │       ├── IEC103Master.h        # 主接口类
│   │       ├── types/
│   │       │   ├── Types.h           # 基础类型定义
│   │       │   ├── DataPoint.h       # 数据点结构
│   │       │   ├── TimeStamp.h       # 7字节时标
│   │       │   └── Constants.h       # 常量定义
│   │       ├── apci/
│   │       │   ├── APCI.h            # APCI结构
│   │       │   ├── Frame.h           # 帧结构
│   │       │   └── SeqManager.h      # 序号管理
│   │       ├── asdu/
│   │       │   ├── ASDU.h            # ASDU结构
│   │       │   ├── ASDU1.h           # 单点遥信
│   │       │   ├── ASDU7_8.h         # 总召唤
│   │       │   ├── ASDU10_21.h       # 通用服务
│   │       │   └── ASDU42.h          # 双点遥信
│   │       └── callback/
│   │           └── IDataHandler.h    # 数据回调接口
│   ├── src/
│   │   ├── apci/
│   │   │   ├── FrameCodec.cpp        # 帧编解码
│   │   │   └── SeqManager.cpp        # 序号管理
│   │   ├── asdu/
│   │   │   ├── ASDUParser.cpp        # ASDU解析
│   │   │   ├── GenericService.cpp    # 通用服务
│   │   │   └── DataParser.cpp        # 数据解析
│   │   ├── transport/
│   │   │   ├── TcpTransport.cpp      # TCP传输
│   │   │   └── Reconnector.cpp       # 重连机制
│   │   ├── master/
│   │   │   └── IEC103Master.cpp      # 主类实现
│   │   └── utils/
│   │       ├── TimeStamp.cpp         # 时标处理
│   │       └── Logger.cpp            # 日志
│   └── CMakeLists.txt
├── app/                              # 控制台应用程序
│   ├── main.cpp                      # 主入口
│   ├── Config.h                      # 配置解析
│   ├── Config.cpp
│   ├── DataPrinter.h                 # 数据打印处理器
│   ├── DataPrinter.cpp
│   └── CMakeLists.txt
├── tests/                            # 测试
│   └── python/                       # Python 子站模拟器
│   │   ├── slave.py                  # 子站模拟器主程序
│   │   ├── protocol/                 # 协议实现
│   │   │   ├── __init__.py
│   │   │   ├── apci.py               # APCI编解码
│   │   │   ├── asdu.py               # ASDU编解码
│   │   │   └── frame.py              # 帧处理
│   │   ├── data/                     # 模拟数据
│   │   │   ├── __init__.py
│   │   │   ├── digital.py            # 遥信数据
│   │   │   └── generic.py            # 通用服务数据(遥测/遥脉)
│   │   └── requirements.txt          # Python依赖
├── docs/                             # 文档
│   └── AGENTS.md
├── IEC60870-5/                       # IEC标准参考文档
├── 上海许继(IEC103南网)/              # 南网规范文档
├── CMakeLists.txt                    # 根CMake配置
├── .gitignore
└── README.md
```

### 4.3 构建产物

```
build/
├── lib/
│   └── iec103.lib (或 libiec103.a)   # 静态库
└── app/
    └── xuji103.exe                   # 控制台程序
```

---

## 五、核心类设计

### 5.1 主接口类 IEC103Master

```cpp
class IEC103Master : public QObject {
    Q_OBJECT
public:
    struct Config {
        QString host;
        quint16 port = 2404;
        int reconnectInterval = 5000;  // ms
        int timeout = 15000;           // T1超时
        int ackTimeout = 10000;        // T2超时
        int testInterval = 20000;      // T3测试间隔
        int maxUnconfirmed = 12;       // k值: 最大未确认I格式APDU数
                                       // 发送方: 未确认数≥k时暂停发送
        int maxAckDelay = 8;           // w值: 最迟确认APDU数
                                       // 接收方: 收到w个I帧后立即发S帧确认
    };

    explicit IEC103Master(QObject* parent = nullptr);
    ~IEC103Master();

    // 连接管理
    void setConfig(const Config& config);
    void connectToServer();
    void disconnectFromServer();
    bool isConnected() const;

    // 数据召唤
    void generalInterrogation(quint16 deviceAddr = 0);  // 总召唤
    void readGenericGroup(quint16 deviceAddr, quint8 group);  // 读通用组

    // 数据回调
    void setDataHandler(IDataHandler* handler);

signals:
    void connected();
    void disconnected();
    void errorOccurred(const QString& error);
    void giFinished(quint16 deviceAddr);

private:
    class Impl;
    std::unique_ptr<Impl> d;
};
```

### 5.2 数据回调接口

```cpp
class IDataHandler {
public:
    virtual ~IDataHandler() = default;

    // ========== 遥信回调 ==========

    // 双点遥信 (总召唤响应ASDU42或突发ASDU1)
    // 南网规范统一使用双点遥信，单点由子站转换为双点
    virtual void onDoublePoint(const DigitalPoint& point) {}

    // ========== 通用服务回调 (遥测/遥脉统一) ==========

    // 通用服务数据 (ASDU10)
    // dataType由GDD.DataType决定: 7=浮点数(遥测), 3=无符号整数(遥脉)
    virtual void onGenericValue(const GenericPoint& point) {}

    // 通用分类数据 (原始格式) - 用于自定义解析
    virtual void onGenericData(uint16_t deviceAddr, const GenericDataItem& item) {}

    // ========== 链路状态回调 ==========

    // 链路状态变化
    virtual void onLinkStateChanged(LinkState state) {}

    // 连接成功
    virtual void onConnected() {}

    // 断开连接
    virtual void onDisconnected() {}

    // 发生错误
    virtual void onError(const QString& error) {}

    // ========== 总召唤回调 ==========

    // 总召唤开始
    virtual void onGIStarted(uint16_t deviceAddr) {}

    // 总召唤完成
    virtual void onGICompleted(uint16_t deviceAddr) {}
};
```

### 5.3 数据结构定义

#### 5.3.1 数据质量标志

```cpp
struct Quality {
    bool invalid = false;      // IV: 无效
    bool notTopical = false;   // NT: 非当前值
    bool substituted = false;  // SB: 被取代
    bool blocked = false;      // BL: 被封锁
    bool overflow = false;     // OV: 溢出（遥测专用）

    bool isGood() const { return !invalid && !notTopical && !substituted && !blocked; }
    static Quality fromByte(quint8 byte);
};

// 双点遥信值
enum class DoublePointValue : uint8_t {
    Indeterminate0 = 0,  // 不确定
    Off = 1,             // 分/开
    On = 2,              // 合/关
    Indeterminate3 = 3   // 不确定
};
```

#### 5.3.2 遥信数据点 (DigitalPoint)

```cpp
struct DigitalPoint {
    uint16_t deviceAddr = 0;      // 设备地址
    uint8_t fun = 0;              // 功能类型
    uint8_t inf = 0;              // 信息序号
    DoublePointValue value = DoublePointValue::Indeterminate0;
    Quality quality;
    QDateTime eventTime;          // 事件发生时间
    QDateTime recvTime;           // 子站接收时间

    // 便捷方法
    bool isOn() const { return value == DoublePointValue::On; }
    bool isOff() const { return value == DoublePointValue::Off; }
    bool isValid() const { return quality.isGood(); }
};
```

#### 5.3.3 通用服务数据点 (GenericPoint)

```cpp
struct GenericPoint {
    uint16_t deviceAddr = 0;      // 设备地址
    uint8_t group = 0;            // 组号
    uint8_t entry = 0;            // 条目号
    uint8_t dataType = 0;         // GDD.DataType: 7=浮点, 3=整数
    QVariant value;               // 根据dataType解析的值
    Quality quality;
    QString unit;                 // 单位

    // 便捷方法
    bool isFloat() const { return dataType == 7; }
    bool isInteger() const { return dataType == 3; }
    float toFloat() const { return value.toFloat(); }
    uint32_t toUInt32() const { return value.toUInt(); }
};
```

### 5.4 时标结构 CP56Time2a

```cpp
#pragma pack(push, 1)
struct CP56Time2a {
    uint16_t millisecond;  // 毫秒 (0-59999)
    uint8_t minute : 6;    // 分钟 (0-59)
    uint8_t iv : 1;        // 无效标志
    uint8_t res1 : 1;
    uint8_t hour : 5;      // 小时 (0-23)
    uint8_t su : 1;        // 夏令时
    uint8_t res2 : 2;
    uint8_t day : 5;       // 日期 (1-31)
    uint8_t weekday : 3;   // 星期 (1-7)
    uint8_t month : 4;     // 月份 (1-12)
    uint8_t res3 : 4;
    uint8_t year;          // 年 (0-99)

    QDateTime toDateTime() const;
    static CP56Time2a fromDateTime(const QDateTime& dt);
};
#pragma pack(pop)
```

---

## 六、帧格式定义

### 6.1 APDU结构

> **南网规范帧格式（参照104标准）：无校验和，无结束字节**

```
┌──────┬──────────┬──────────┬─────────────────────────┐
│ 68H  │ 长度L    │ 长度L    │ APCI (4字节) + ASDU     │
│      │ 低字节   │ 高字节   │                         │
└──────┴──────────┴──────────┴─────────────────────────┘

长度L = APCI长度(4) + ASDU长度
最大长度 = 2045 (APDU_MAX = 2048 - 启动字符(1) - 长度域(2字节))
```

### 6.2 ASDU结构

```
┌──────────┬──────────┬──────────┬──────────┬──────────────────┐
│ TI       │ VSQ      │ COT      │ ASDU地址 │ 信息对象         │
│ (1字节)  │ (1字节)  │ (1字节)  │ (2字节)  │ (可变)           │
└──────────┴──────────┴──────────┴──────────┴──────────────────┘

TI: 类型标识
VSQ: 可变结构限定词 (SQ位 + 信息对象数目)
COT: 传输原因 (1字节，与标准103一致)
```

### 6.3 主要ASDU类型（本项目实现范围）

> **注意**：本项目仅实现三遥数据采集，不实现控制功能

| ASDU | TI | 方向 | 用途 | 实现状态 |
|------|-----|------|------|----------|
| **遥信相关** |||||
| ASDU1 | 01H | 监视 | 带时标的报文（突发上送，含DPI） | ✓ 已实现 |
| ASDU2 | 02H | 监视 | 带相对时间的时标报文（保护动作事件） | ✓ 已实现 |
| **ASDU42** | **2AH** | 监视 | **双点信息状态（总召唤响应，南网扩展）** | ✓ 已实现 |
| **遥测/遥脉** |||||
| ASDU10 | 0AH | 监视 | 通用分类数据（遥测/遥脉上送） | ✓ 已实现 |
| ASDU21 | 15H | 控制 | 通用分类读命令（召唤遥测/遥脉） | ✓ 已实现 |
| **总召唤相关** |||||
| ASDU7 | 07H | 控制 | 总召唤命令 | ✓ 已实现 |
| ASDU8 | 08H | 监视 | 总召唤结束 | ✓ 已实现 |

**不实现的ASDU**（超出项目范围）：
- ASDU6 (时钟同步) - 控制功能
- ASDU12 (录波简报) - 特殊数据
- ASDU13-16 (录波文件) - 特殊数据
- ASDU20 (复归命令) - 控制功能
- ASDU17-18 (历史信息) - 特殊数据
- ASDU101-108 (通用文件) - 特殊数据

---

## 七、传输原因(COT)定义

### 监视方向

| COT | 含义 | 说明 |
|-----|------|------|
| 1 | 自发(突发) | 事件触发上送 |
| 2 | 循环 | 周期性上送 |
| 3 | 复位帧计数位(FCB) | 复位后首次上送 |
| 4 | 复位通信单元(CU) | 复位后首次上送 |
| 5 | 启动/重新启动 | 装置启动 |
| 6 | 电源合上 | 装置上电 |
| 7 | 测试模式 | 测试状态 |
| 8 | 时间同步 | 时间同步响应 |
| **9** | **总召唤** | **GI响应数据** |
| **10** | **总召唤终止** | **GI结束标志** |
| 11 | 当地操作 | 本地操作触发 |
| 12 | 远方操作 | 远方控制触发 |
| 20 | 命令的肯定认可 | 控制命令确认 |
| 21 | 命令的否定认可 | 控制命令拒绝 |
| 31 | 扰动数据的传送 | 录波数据 |
| 40 | 通用分类写命令的肯定认可 | 写成功 |
| 41 | 通用分类写命令的否定认可 | 写失败 |
| 42 | 对通用分类读命令有效数据响应 | 读成功 |
| 43 | 对通用分类读命令无效数据响应 | 读失败 |

### 控制方向

| COT | 含义 | 说明 |
|-----|------|------|
| 8 | 时间同步 | 时间同步命令 |
| **9** | **总召唤启动** | **GI命令** |
| 20 | 一般命令 | 控制命令 |
| 31 | 扰动数据的传输 | 录波请求 |
| 40 | 通用分类写命令 | 写定值 |
| 42 | 通用分类读命令 | 读数据 |

---

## 八、功能类型(FUN)定义

### 兼容范围

| FUN | 名称 | 说明 |
|-----|------|------|
| 128 | t(Z) | 距离保护 |
| 160 | I>> | 过流保护 |
| 176 | IT | 变压器差动保护 |
| 192 | IL | 线路差动保护 |
| **254** | **GEN** | **通用分类功能类型** |
| **255** | **GLB** | **全局功能类型** |

> **本项目主要使用**：GEN(254)用于通用服务，GLB(255)用于系统功能

---

## 九、信息序号(INF)定义

### 监视方向 - 系统功能(0-15)

| INF | 描述 | TYP | COT | FUN |
|-----|------|-----|-----|-----|
| **0** | **总召唤结束** | 8 | 10 | GLB |
| 0 | 时间同步 | 6 | 8 | GLB |

### 监视方向 - 状态信号(16-31)

| INF | 描述 | GI | TYP | COT |
|-----|------|-----|-----|-----|
| 16 | 自动重合闸投运 | × | 1 | 1,7,9,11,12,20,21 |
| 17 | 远方保护投运 | × | 1 | 1,7,9,11,12,20,21 |
| 18 | 继电保护投运 | × | 1 | 1,7,9,11,12,20,21 |
| 19 | LED复位 | - | 1 | 1,7,11,12,20,21 |
| 20 | 监视方向信息闭锁 | × | 1 | 9,11 |
| 21 | 测试模式 | × | 1 | 9,11 |
| 22 | 当地参数设置 | × | 1 | 9,11 |

### 监视方向 - 通用分类功能(240-255)

| INF | 描述 | TYP | COT | FUN |
|-----|------|-----|-----|-----|
| 240 | 读全部被定义组的标题 | 10 | 42,43 | GEN |
| **241** | **读一个组的全部条目** | **10** | **42,43** | **GEN** |
| 243 | 读单个条目的目录 | 11 | 42,43 | GEN |
| 244 | 读单个条目的值或属性 | 10 | 1,9,11,42,43 | GEN |
| 245 | 通用分类数据总召唤中止 | 10 | 10 | GEN |
| 249 | 带确认的写条目 | 10 | 41,44 | GEN |
| 250 | 带执行的写条目 | 10 | 40,41 | GEN |

### 控制方向 - 系统功能(0-15)

| INF | 描述 | TYP | COT | FUN |
|-----|------|-----|-----|-----|
| **0** | **总召唤启动** | **7** | **9** | **GLB** |

### 控制方向 - 通用分类功能(240-255)

| INF | 描述 | TYP | COT | FUN |
|-----|------|-----|-----|-----|
| 240 | 读全部被定义组的标题 | 21 | 42 | GEN |
| **241** | **读一个组的全部条目** | **21** | **42** | **GEN** |
| 243 | 读单个条目的目录 | 21 | 42 | GEN |
| 244 | 读单个条目的值或属性 | 21 | 42 | GEN |
| 245 | 通用分类数据总召唤 | 21 | 9 | GEN |

> **GI列说明**：× 表示该信息包含在总召唤范围内

---

## 十、ASDU信息对象格式

### 10.1 ASDU1 - 带时标的报文（南网规范）

```
┌─────────────────────────────────────────────────────────┐
│ 数据单元标识符 (4字节)                                   │
│ TI=01H | VSQ | COT | ASDU地址                           │
├─────────────────────────────────────────────────────────┤
│ 信息体                                                  │
│ ├─ FUN (1字节) - 功能类型                               │
│ ├─ INF (1字节) - 信息序号                               │
│ ├─ DPI (1字节) - 双点信息                               │
│ │    D7-D2: 备用(0)                                     │
│ │    D1-D0: 00=未用, 01=分/OFF, 10=合/ON, 11=未用       │
│ ├─ CP56Time2a (7字节) - 实际发生时间（南网扩展）         │
│ ├─ CP56Time2a (7字节) - 子站接收时间（南网扩展）         │
│ └─ SIN (1字节) - 附加信息                               │
└─────────────────────────────────────────────────────────┘
```

> **南网扩展**：时标使用7字节CP56Time2a，且包含两个时标（实际发生时间+子站接收时间）

### 10.2 ASDU2 - 带相对时间的时标报文（南网规范）

```
┌─────────────────────────────────────────────────────────┐
│ 数据单元标识符 (4字节)                                   │
│ TI=02H | VSQ | COT | ASDU地址                           │
├─────────────────────────────────────────────────────────┤
│ 信息体                                                  │
│ ├─ FUN (1字节) - 功能类型                               │
│ ├─ INF (1字节) - 信息序号                               │
│ ├─ DPI (1字节) - 双点信息                               │
│ ├─ RET (2字节) - 相对时间(ms)                           │
│ ├─ FAN (2字节) - 故障序号                               │
│ ├─ CP56Time2a (7字节) - 实际发生时间（南网扩展）         │
│ ├─ CP56Time2a (7字节) - 子站接收时间（南网扩展）         │
│ └─ SIN (1字节) - 附加信息（FPT故障相别及类型）           │
└─────────────────────────────────────────────────────────┘
```

> **南网扩展**：SIN字段用于表示故障相别及类型(FPT)

### 10.3 ASDU7 - 总召唤命令

```
┌─────────────────────────────────────────────────────────┐
│ 数据单元标识符 (4字节)                                   │
│ TI=07H | VSQ=01H | COT=09H | ASDU地址                   │
├─────────────────────────────────────────────────────────┤
│ 信息体                                                  │
│ ├─ FUN (1字节) = GLB(255)                               │
│ ├─ INF (1字节) = 0                                      │
│ └─ SCN (1字节) - 扫描序号                               │
└─────────────────────────────────────────────────────────┘
```

### 10.4 ASDU8 - 总召唤结束

```
┌─────────────────────────────────────────────────────────┐
│ 数据单元标识符 (4字节)                                   │
│ TI=08H | VSQ=01H | COT=0AH | ASDU地址                   │
├─────────────────────────────────────────────────────────┤
│ 信息体                                                  │
│ ├─ FUN (1字节) = GLB(255)                               │
│ ├─ INF (1字节) = 0                                      │
│ └─ SCN (1字节) - 扫描序号(与命令相同)                    │
└─────────────────────────────────────────────────────────┘
```

### 10.5 ASDU42 - 双点信息状态（南网扩展，总召唤响应）

```
┌─────────────────────────────────────────────────────────┐
│ 数据单元标识符 (4字节)                                   │
│ TI=2AH | VSQ | COT=09H | ASDU地址                       │
├─────────────────────────────────────────────────────────┤
│ 信息体（可包含多个双点信息）                             │
│ ├─ FUN (1字节) - 功能类型                               │
│ ├─ INF (1字节) - 信息序号                               │
│ ├─ DPI (1字节) - 双点信息                               │
│ ├─ FUN (1字节) - 功能类型                               │
│ ├─ INF (1字节) - 信息序号                               │
│ ├─ DPI (1字节) - 双点信息                               │
│ │   ... (可重复多个)                                    │
│ └─ SIN (1字节) - 扫描序号SCN                            │
└─────────────────────────────────────────────────────────┘
```

> **南网扩展**：ASDU42用于总召唤时上送持续性状态量的当前值

---

## 十一、关键信息元素定义

### 11.1 VSQ - 可变结构限定词

```
┌─────────────────────────────────────────────────────────┐
│ D7 (SQ位):                                               │
│   0 = 信息体地址寻址顺序的信息元素                       │
│   1 = 寻址单个信息元素或综合信息元素                     │
│ D6-D0 (数目): 信息对象数目 (0-127)                       │
└─────────────────────────────────────────────────────────┘
```

### 11.2 DPI - 双点信息

```
┌─────────────────────────────────────────────────────────┐
│ D1-D0:                                                   │
│   00 = 未用/不确定                                       │
│   01 = 分/开 (OFF)                                       │
│   10 = 合/关 (ON)                                        │
│   11 = 未用/不确定                                       │
└─────────────────────────────────────────────────────────┘
```

**南网规范DPI含义**：

| 信息类型 | DPI=01 | DPI=10 | DPI=00/11 |
|----------|--------|--------|-----------|
| 开关量 | 开/OFF | 合/ON | 不确定 |
| 保护动作信号 | 动作复归/OFF | 动作出口/ON | 不确定 |
| 压板状态 | 未投入/OFF | 投入/ON | 不确定 |
| 自检信号/其他事件 | 告警复归/OFF | 告警产生/ON | 不确定 |
| 运行状态 | 检修 | 运行 | 不确定 |

### 11.3 FPT - 故障相别及类型（南网扩展）

```
┌─────────────────────────────────────────────────────────┐
│ D7: 有效位 (1=有效, 0=无效)                              │
│ D6-D5: 保留                                              │
│ D4: 区内(1)/区外(0)故障                                  │
│ D3: 接地(1)/非接地(0)故障                                │
│ D2: C相故障                                              │
│ D1: B相故障                                              │
│ D0: A相故障                                              │
└─────────────────────────────────────────────────────────┘
```

**示例**：
- 1000,1001 = A相接地故障
- 1000,0011 = AB相间故障
- 1001,0000 = 区外故障，类型未知

### 11.4 SIN - 附加信息

```
┌─────────────────────────────────────────────────────────┐
│ 当 COT=总召唤时: SIN = 扫描序号(SCN)                     │
│ 当 COT=命令认可时: SIN = 返回信息标识符(RII)             │
│ 当 COT=其他时: SIN = 无关                                │
└─────────────────────────────────────────────────────────┘
```

### 11.5 MEA - 带品质描述词的被测值

```
┌─────────────────────────────────────────────────────────┐
│ D15 (OV): 溢出标志 (0=无溢出, 1=溢出)                    │
│ D14 (ER): 差错标志 (0=有效, 1=无效)                      │
│ D13 (RES): 备用 (常为0)                                  │
│ D12-D0: 被测值 (F13格式, -1~+1, 比例因子2^12)            │
└─────────────────────────────────────────────────────────┘
```

---

## 十二、南网规范扩展

> **重要说明**：南网以太网103规范在标准IEC 60870-5-103基础上进行了扩展

### 12.1 ASDU地址扩展

南网规范将ASDU地址从1字节扩展为2字节：

```
┌─────────────────────────────────────────────────────────┐
│ 高字节: 设备地址 (1-254)                                 │
│ 低字节: D7-D4=定值区号, D2-D0=CPU号                      │
├─────────────────────────────────────────────────────────┤
│ 特殊地址:                                                │
│   0x0000 = 子站本身                                      │
│   0x00FF = 广播地址                                      │
│   0xFF00-0xFFFE = 定值区号+装置                          │
└─────────────────────────────────────────────────────────┘
```

### 12.2 时标扩展

南网规范使用7字节时标CP56Time2a替代4字节CP32Time2a：

```
┌─────────────────────────────────────────────────────────┐
│ 字节1-2: 毫秒 (0-59999)                                  │
│ 字节3: D0-D5=分钟(0-59), D6=IV无效, D7=备用              │
│ 字节4: D0-D4=小时(0-23), D5=SU夏令时, D6-D7=备用         │
│ 字节5: D0-D4=日(1-31), D5-D7=星期(1-7)                   │
│ 字节6: D0-D3=月(1-12), D4-D7=备用                        │
│ 字节7: 年(0-99)                                          │
└─────────────────────────────────────────────────────────┘
```

### 12.3 通用服务数据类型扩展

南网扩展的数据类型（用于ASDU10）：

| DataType | 说明 | 用途 |
|----------|------|------|
| **213** | 带绝对时间的七字节时标报文 | 遥信/事件 |
| **214** | 带相对时间的七字节时标报文 | 保护动作事件 |
| **215** | 带相对时间七字节时标的浮点值 | 故障量（遥测） |
| **216** | 带相对时间七字节时标的整形值 | 故障量（整型） |
| **217** | 带相对时间七字节时标的字符值 | 故障量（字符） |

### 12.4 KOD扩展

南网扩展的描述类别：

| KOD | 含义 | 说明 |
|-----|------|------|
| **01H** | **从装置取实际值** | 南网扩展，立即从装置读取 |
| **67H** | 属性结构 | 南网扩展，包含完整属性 |
| **168(0xA8)H** | **从子站数据库取实际值** | 南网扩展，从子站缓存读取 |
| **169(0xA9)H** | 从子站数据库读取 | 南网扩展 |

> **重要**：读模拟量时，KOD=01H表示从装置实时读取，KOD=168(0xA8)H表示从子站数据库读取

---

## 十三、主站通信流程

### 13.1 整体流程图

```
┌─────────────────────────────────────┐
│          初始化过程                  │
│  TCP连接 → STARTDT启动              │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      加载配置文件                    │
│  (组号、召唤间隔等)                  │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      总召唤时间到？                  │
│        ↓是        ↓否              │
│   总召唤(遥信)    │                 │
│   ASDU7→42→8     │                 │
│        ↓←─────────┘                 │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│    通用服务召唤时间到？              │
│        ↓是        ↓否              │
│  读配置的组(遥测/遥脉)              │
│   ASDU21→10       │                 │
│        ↓←─────────┘                 │
└─────────────────────────────────────┘
                  ↓
        ┌─────────────────┐
        │  循环回到起点    │
        └─────────────────┘
```

> **说明**：遥测和遥脉统一通过通用服务召唤，组号从配置文件读取。

### 13.2 总召唤流程（遥信）

```
主站                                    子站
  │                                       │
  │───── ASDU7 (总召唤命令) ────────────>│
  │        COT=09, SCN=扫描序号           │
  │                                       │
  │<──── ASDU42 (双点信息状态) ──────────│
  │        COT=09, 多个信息对象            │
  │        FUN/INF/DPI/SIN                │
  │                                       │
  │<──── ASDU42 (双点信息状态) ──────────│
  │        ... (多帧)                     │
  │                                       │
  │<──── ASDU8 (总召唤结束) ─────────────│
  │        COT=0A, SCN=扫描序号           │
  │                                       │
```

> **注意**：总召唤仅上送持续性状态量（开关量、压板状态、通讯状态等）

### 13.3 通用服务召唤流程（遥测/遥脉）

> **组号从配置文件读取，数据类型由GDD.DataType决定**

```
主站                                    子站
  │                                       │
  │───── ASDU21 (读一组全部条目) ───────>│
  │        FUN=FE, INF=F1                 │
  │        GIN=组号(从配置读取)            │
  │        KOD=01(实际值)                 │
  │                                       │
  │<──── ASDU10 (通用分类数据) ──────────│
  │        FUN=FE, INF=F1                 │
  │        NGD=条目数                     │
  │        GIN/KOD/GDD/GID...             │
  │        GDD.DataType决定数据类型:      │
  │          7 = 浮点数(遥测)             │
  │          3 = 无符号整数(遥脉)         │
  │                                       │
```

> **注意**：遥测和遥脉使用相同的流程，区别仅在于：
> - 组号不同（从配置文件读取）
> - 数据类型不同（由GDD.DataType决定）

---

## 十四、通用服务详解

### 14.1 通用服务结构

```
ASDU10 (通用分类数据) / ASDU21 (通用分类读命令):

┌──────┬──────┬──────┬──────────┬──────┬──────┬─────────────┐
│ TI   │ VSQ  │ COT  │ ASDU地址 │ FUN  │ INF  │ 数据内容    │
│ 0A/15│      │      │          │ FE   │ F0-F4│             │
└──────┴──────┴──────┴──────────┴──────┴──────┴─────────────┘

FUN=FEH (通用分类功能类型)
INF (本项目使用的读命令):
  F0H = 读全部组的标题
  F1H = 读一组的全部条目
  F3H = 读单个条目的目录
  F4H = 读单个条目的值
```

> **注意**：INF F9H(带确认写)、FAH(带执行写)为定值修改功能，本项目不实现

### 14.2 KOD描述类别

| KOD | 含义 | 说明 |
|-----|------|------|
| 01H | 实际值 | 从装置读取 |
| 0AH | 描述 | 名称/描述字符串 |
| 67H | 属性结构 | 南网扩展，包含完整属性 |
| A8H | 从装置读取实际值 | 南网扩展 |
| A9H | 从子站数据库读取 | 南网扩展 |

### 14.3 数据类型定义（南网扩展）

| DataType | 说明 | 用途 |
|----------|------|------|
| 1 | OS8ASCII 字符串 | 描述、名称 |
| 3 | 无符号整数 | 整数值 |
| 5 | 有符号整数 | 有符号值 |
| 7 | R32.23 浮点数 | **遥测值** |
| 22 | 通用分类失败回答码 | 错误响应 |
| 23 | 数据结构 | 复合结构 |
| **213** | 带绝对时间的七字节时标报文 | **遥信/事件** |
| **214** | 带相对时间的七字节时标报文 | **保护动作事件** |
| **215** | 带相对时间七字节时标的浮点值 | **故障量（遥测）** |
| **216** | 带相对时间七字节时标的整形值 | **故障量（整型）** |
| **217** | 带相对时间七字节时标的字符值 | **故障量（字符）** |

### 14.4 通用分类数据结构

#### 14.4.1 ASDU21（通用分类读命令 - 控制方向）

```
┌─────────────────────────────────────────────────────┐
│ RII (返回信息标识符) - 1字节                         │
│ NGD (通用分类标识数目) - 1字节                       │
│   ┌─ CONT: 后续位                                    │
│   └─ 数目: 本帧请求数                                │
├─────────────────────────────────────────────────────┤
│ 请求数据 (重复NGD次):                                │
│   ┌─────────────────────────────────────────────┐   │
│   │ GIN (通用分类标识序号) - 2字节               │   │
│   │   组号 (1字节) + 条目号 (1字节)              │   │
│   ├─────────────────────────────────────────────┤   │
│   │ KOD (描述类别) - 1字节                       │   │
│   │   01H = 从装置取实际值                       │   │
│   │   A8H = 从子站数据库取实际值 (南网扩展)      │   │
│   └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘

注意：ASDU21（读命令）到KOD就结束，不包含GDD和GID！
```

#### 14.4.2 ASDU10（通用分类数据 - 监视方向）

```
┌─────────────────────────────────────────────────────┐
│ RII (返回信息标识符) - 1字节                         │
│ NGD (通用分类数据集数目) - 1字节                     │
│   ┌─ CONT: 后续位                                    │
│   └─ 数目: 本帧数据集数                              │
├─────────────────────────────────────────────────────┤
│ 数据集 (重复NGD次):                                  │
│   ┌─────────────────────────────────────────────┐   │
│   │ GIN (通用分类标识序号) - 2字节               │   │
│   │   组号 (1字节) + 条目号 (1字节)              │   │
│   ├─────────────────────────────────────────────┤   │
│   │ KOD (描述类别) - 1字节                       │   │
│   ├─────────────────────────────────────────────┤   │
│   │ GDD (通用分类数据描述) - 3字节               │   │
│   │   DataType (1字节) + DataSize (1字节)        │   │
│   │   + Number (1字节)                           │   │
│   ├─────────────────────────────────────────────┤   │
│   │ GID (通用分类标识数据) - 可变长度            │   │
│   │   长度 = DataSize × Number                   │   │
│   └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘

注意：ASDU10（响应）包含完整的GIN + KOD + GDD + GID结构
```

---

## 十五、开发阶段

### Phase 1: 库基础架构
- [x] 项目结构搭建 (lib/app/tests分离)
- [x] CMake配置 (静态库 + 控制台程序)
- [x] 基础类型定义 (types/)

### Phase 2: APCI基础层
- [x] APDU帧结构定义
- [x] 帧编解码 (FrameCodec)
- [x] 序号管理器 (SeqManager)
- [x] I/S/U格式处理
- [x] **k值发送控制** (未确认帧数≥k时暂停发送)
- [x] **w值确认触发** (收到w个I帧后立即发S帧)

### Phase 3: ASDU层（三遥解析）
- [x] ASDU基础结构
- [x] CP56Time2a时标处理
- [x] **遥信解析**: ASDU1 (突发) / ASDU42 (总召唤)
- [x] **通用服务解析**: ASDU10 (遥测/遥脉统一)
- [x] Quality质量标志解析

### Phase 4: 通用服务
- [x] ASDU10/21 编解码
- [x] GIN/KOD/GDD/GID结构
- [x] 数据类型213-217解析
- [x] 读一组全部条目 (INF=F1)

### Phase 5: 传输层
- [x] TCP传输封装
- [x] STARTDT/STOPDT/TESTFR
- [x] 断线重连机制
- [x] 超时处理 (T1/T2/T3)

### Phase 6: 库API层
- [x] IEC103Master主类
- [x] **总召唤流程** (ASDU7→ASDU42→ASDU8)
- [x] **通用服务召唤** (ASDU21→ASDU10，遥测/遥脉统一)
- [x] 数据回调机制

### Phase 7: 控制台程序
- [x] 命令行参数解析
- [x] 配置文件加载
- [x] DataPrinter数据输出
- [x] 日志系统

### Phase 8: Python测试子站
- [x] TCP服务器框架
- [x] APCI编解码 (apci.py)
- [x] ASDU编解码 (asdu.py)
- [x] 模拟数据生成 (data/)
- [x] 总召唤响应
- [x] 通用服务响应

### Phase 9: 测试与文档
- [x] 集成测试 (主站+Python子站)
- [ ] API文档

### Phase 10: 安全性与稳定性修复 (2026-03-09)

#### P0 安全性修复
- [x] **帧长度校验**: 检测超长帧(>2045字节)并断开连接
- [x] **接收缓冲区限制**: 防止内存耗尽攻击

#### P1 稳定性修复
- [x] **T0连接超时**: 30秒连接建立超时
- [x] **T1命令超时扩展**: GI命令和读命令超时保护
- [x] **DataType 213-217解析**: 南网扩展数据类型完整支持
- [x] **KOD 67H属性结构**: 基本解析支持

#### P2 可靠性增强
- [x] **ASDU11通用分类标识**: 读单个条目目录响应
- [x] **重连次数限制**: 默认无限重连(适合后台服务)
- [x] **链路断开通知完善**: 带原因的断开回调
- [x] **发送队列**: 未确认I帧缓存
- [x] **k值阻塞超时告警**: 防止死锁
- [x] **配置参数校验**: 参数范围检查

#### 测试验证
- [x] 正常模式测试 (总召唤、通用服务、w值确认)
- [x] no_ack模式测试 (k值阻塞)
- [x] oversized_frame模式测试 (帧长度校验)
- [x] ext_types模式测试 (DataType 213-217)
- [x] 无限重连验证

---

## 十六、本项目不实现的功能

> 根据需求，以下功能不在实现范围内：

| 功能 | ASDU | 说明 |
|------|------|------|
| 控制命令 | ASDU6, ASDU20 | 时钟同步、复归 |
| 定值操作 | ASDU10 INF=F9/FA | 写定值、切换定值区 |
| 录波文件 | ASDU12-16 | 简要录波、文件传输 |
| 通用文件 | ASDU101-108 | 文件传输 |
| 故障历史 | ASDU17-18 | 历史信息召唤 |
| 配置读取 | ASDU21 INF=F0-F4 | 可选，按需实现 |

---

## 十七、技术栈

### C++ 库与程序

| 组件 | 版本/说明 |
|------|----------|
| 语言 | C++17 |
| 框架 | Qt5 Core + Network |
| 构建系统 | CMake 3.5+ |
| 生成器 | Ninja |
| IDE | Visual Studio Code |
| 调试器 | cppvsdbg (Windows) |

### Python 测试子站

| 组件 | 版本/说明 |
|------|----------|
| 语言 | Python 3.7+ |
| 异步框架 | asyncio |
| 类型检查 | dataclasses |

---

## 十八、构建与运行

### 构建项目
```powershell
cd D:\project\C++\xuji103
cmake -B build -G Ninja
cmake --build build
```

### 运行控制台程序
```powershell
# 显示帮助
.\build\app\xuji103.exe --help

# 连接子站并执行总召唤
.\build\app\xuji103.exe --host 192.168.1.100 --port 2404

# 使用配置文件
.\build\app\xuji103.exe --config config.json

# 持续轮询模式
.\build\app\xuji103.exe --host 192.168.1.100 --poll-interval 5000
```

### 命令行参数
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--host` | 子站IP地址 | 127.0.0.1 |
| `--port` | 子站端口 | 2404 |
| `--config` | 配置文件路径 | - |
| `--poll-interval` | 轮询间隔(ms) | 5000 |
| `--gi-interval` | 总召唤间隔(ms) | 60000 |
| `--log-level` | 日志级别 | info |
| `--help` | 显示帮助 | - |

### 运行Python测试子站
```powershell
cd tests\python
pip install -r requirements.txt
python slave.py --port 2404
```

### 调试
在 VS Code 中按 `F5` 启动调试。

---

## 十九、开发约定

### 编码规范
- 使用 C++17 标准
- Qt 信号槽机制使用新式语法
- 遵循 Qt 编码风格

### 构建配置
- CMAKE_AUTOUIC、CMAKE_AUTOMOC、CMAKE_AUTORCC 已启用
- 输出目录：`build/xuji103.exe`

---

## 二十、依赖库

| 库 | 用途 | 状态 |
|----|------|------|
| Qt5::Core | 核心框架、信号槽 | ✓ 已集成 |
| Qt5::Network | TCP通信 | ✓ 已集成 |
| Qt5::Test | 单元测试 | 可选 |

---

## 二十一、使用示例

### 21.1 库的使用方式

```cpp
#include <iec103/IEC103Master.h>

class MyHandler : public IDataHandler {
public:
    // 遥信回调（双点）- 总召唤响应ASDU42或突发ASDU1
    void onDoublePoint(const DigitalPoint& point) override {
        qDebug() << "遥信: 装置=" << point.deviceAddr
                 << "FUN=" << point.fun << "INF=" << point.inf
                 << (point.isOn() ? "合" : "分")
                 << "时间:" << point.eventTime.toString();
    }

    // 通用服务回调（遥测/遥脉统一）- ASDU10
    // dataType由GDD.DataType决定: 7=浮点(遥测), 3=整数(遥脉)
    void onGenericValue(const GenericPoint& point) override {
        if (point.isFloat()) {  // 浮点数 (遥测)
            qDebug() << "遥测: 装置=" << point.deviceAddr
                     << "组=" << point.group << "条目=" << point.entry
                     << "=" << point.toFloat() << point.unit;
        } else if (point.isInteger()) {  // 无符号整数 (遥脉)
            qDebug() << "遥脉: 装置=" << point.deviceAddr
                     << "组=" << point.group << "条目=" << point.entry
                     << "=" << point.toUInt32() << point.unit;
        }
    }

    // 总召唤完成
    void onGICompleted(uint16_t dev) override {
        qDebug() << "总召唤完成: 装置=" << dev;
    }
};

int main(int argc, char *argv[]) {
    QCoreApplication app(argc, argv);

    IEC103Master master;
    MyHandler handler;

    master.setDataHandler(&handler);
    master.setConfig({"192.168.1.100", 2404});
    master.connectToServer();

    // 连接成功后执行召唤
    QObject::connect(&master, &IEC103Master::connected, [&]() {
        master.generalInterrogation(0);  // 总召唤（遥信）
        // 召唤通用服务组（组号从配置读取）
        for (auto group : Config::instance().groups()) {
            master.readGenericGroup(1, group);
        }
    });

    return app.exec();
}
```

### 21.2 控制台程序使用

```powershell
# 基本使用
.\xuji103.exe --host 192.168.1.100 --port 2404

# 使用配置文件
.\xuji103.exe --config config.json
```

配置文件示例 (config.json):
```json
{
  "connection": {
    "host": "192.168.1.100",
    "port": 2404,
    "timeout": 15000,
    "reconnect_interval": 5000
  },
  "polling": {
    "gi_interval": 60000,
    "groups": [
      {"group": 8, "name": "电流电压", "data_type": 7},
      {"group": 9, "name": "功率频率", "data_type": 7},
      {"group": 16, "name": "电能量", "data_type": 3},
      {"group": 17, "name": "运行时间", "data_type": 3}
    ]
  },
  "logging": {
    "level": "info",
    "file": "xuji103.log"
  }
}
```

> **说明**：
> - `groups` 数组定义要召唤的组，组号由子站配置决定
> - `data_type` 用于提示数据类型：7=浮点数(遥测), 3=整数(遥脉)
> - 实际数据类型由响应报文中的GDD.DataType决定

### 21.3 输出示例

```
[2026-03-04 10:30:15.123] [INFO] 连接到 192.168.1.100:2404
[2026-03-04 10:30:15.156] [INFO] STARTDT 已确认
[2026-03-04 10:30:15.200] [INFO] 开始总召唤...
[2026-03-04 10:30:15.234] [DATA] 遥信 装置1 开关1 合 时间:2026-03-04 10:30:15
[2026-03-04 10:30:15.235] [DATA] 遥信 装置1 开关2 分 时间:2026-03-04 10:30:15
[2026-03-04 10:30:15.300] [INFO] 总召唤完成 装置1
[2026-03-04 10:30:16.100] [DATA] 通用服务 装置1 组8 条目1 = 100.5 A (浮点)
[2026-03-04 10:30:16.101] [DATA] 通用服务 装置1 组8 条目2 = 220.3 V (浮点)
[2026-03-04 10:30:17.100] [DATA] 通用服务 装置1 组16 条目1 = 12345 kWh (整数)
```

---

## 二十二、参考文档

1. **南网规范**：继电保护信息系统主站-子站以太网103通信规范 (Q/CSG)
2. **IEC 60870-5-103**: 继电保护设备信息接口
3. **IEC 60870-5-104**: 网络访问协议（APCI参考）
4. **DL/T 634.5104-2009**: 104标准中文版

---

## 二十三、Python测试子站

### 23.1 概述

Python子站模拟器用于测试主站程序，提供以下功能：
- TCP服务器，监听端口2404
- 响应STARTDT/STOPDT/TESTFR控制命令
- 响应总召唤(ASDU7)，返回遥信数据(ASDU42)和结束标志(ASDU8)
- 响应通用服务读命令(ASDU21)，返回遥测/遥脉数据(ASDU10)
- 模拟突发遥信上送(ASDU1)

### 23.2 依赖

```txt
# requirements.txt
asyncio      # 异步IO (Python 3.7+)
dataclasses  # 数据类 (Python 3.7+)
```

### 23.3 使用方法

```powershell
# 启动子站模拟器
cd tests\python
python slave.py --port 2404

# 指定模拟数据配置
python slave.py --port 2404 --data data\sample.json

# 启用调试日志
python slave.py --port 2404 --log-level debug
```

### 23.4 模拟数据配置

```json
// data/sample.json
{
  "devices": [
    {
      "address": 1,
      "name": "测试装置1",
      "digitals": [
        {"fun": 200, "inf": 1, "name": "开关1", "value": "on"},
        {"fun": 200, "inf": 2, "name": "开关2", "value": "off"}
      ],
      "groups": [
        {
          "group": 8,
          "name": "电流电压",
          "entries": [
            {"entry": 1, "name": "电流A相", "value": 100.5, "unit": "A", "data_type": 7},
            {"entry": 2, "name": "电压A相", "value": 220.3, "unit": "V", "data_type": 7}
          ]
        },
        {
          "group": 16,
          "name": "电能量",
          "entries": [
            {"entry": 1, "name": "正向有功电能", "value": 12345, "unit": "kWh", "data_type": 3}
          ]
        }
      ]
    }
  ]
}
```

> **说明**：
> - `digitals`: 遥信数据，用于总召唤(ASDU42)和突发(ASDU1)
> - `groups`: 通用服务数据（遥测/遥脉统一），用于响应ASDU21读命令
> - `data_type`: 数据类型，7=浮点数，3=无符号整数

### 23.5 命令行参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--port` | 监听端口 | 2404 |
| `--host` | 绑定地址 | 0.0.0.0 |
| `--data` | 模拟数据配置文件 | - |
| `--log-level` | 日志级别 | info |
| `--test-mode` | 测试模式 | normal |

### 23.6 测试模式

子站模拟器支持多种测试模式，用于验证主站的各种功能：

```bash
python slave.py --test-mode <模式>
```

| 模式 | 说明 | 测试目标 |
|-----|------|---------|
| `normal` | 正常模式 | 完整通信流程验证 |
| `no_ack` | 不发送S帧确认 | k值阻塞超时机制 |
| `oversized_frame` | 发送超长帧 | 帧长度校验(P0) |
| `no_response` | 不响应命令 | T1命令超时机制 |
| `flood` | 快速发送大量帧 | 接收缓冲区限制(P0) |
| `ext_types` | 发送南网扩展数据类型 | DataType 213-217解析(P1) |

**测试模式核心日志示例：**

```
# oversized_frame模式 - 主站检测超长帧
TcpTransport: Frame length exceeds maximum 2045 , got 3010 - clearing and disconnecting
[ERROR] Transport error: Frame length exceeds maximum: 3010

# ext_types模式 - 主站解析南网扩展类型
[INFO] Generic(float215): Dev=1 Group=100 Entry=1 Value=123.45 DataType=215
[INFO] Generic(int216): Dev=1 Group=100 Entry=2 Value=65535 DataType=216
[INFO] Generic(char217): Dev=1 Group=100 Entry=3 Value="A" DataType=217

# normal模式 - w值确认机制
[INFO] [w-Control] I-Frame #3 received, w=3, threshold=REACHED
[INFO] [S-TX] Sent S-Frame N(R)=3 (acknowledged peer's I-frames)
```

---

*最后更新: 2026-03-09*
